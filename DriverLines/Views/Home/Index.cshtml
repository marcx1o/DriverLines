@{
    ViewData["Title"] = "الصفحة الرئيسية";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<style>
    body {
        direction: rtl;
        font-family: 'Cairo', sans-serif;
        background-color: #f6f8fb;
        color: #182848;
    }

    .home-container {
        max-width: 900px;
        margin: 80px auto;
        text-align: center;
        background: #fff;
        padding: 50px 40px;
        border-radius: 20px;
        box-shadow: 0 6px 24px rgba(0, 0, 0, 0.08);
        transition: 0.3s ease-in-out;
    }

        .home-container:hover {
            transform: scale(1.01);
            box-shadow: 0 8px 28px rgba(0, 0, 0, 0.12);
        }

    h1 {
        font-size: 2.2rem;
        font-weight: 700;
        color: #182848;
        margin-bottom: 20px;
    }

    p {
        font-size: 1.1rem;
        color: #555;
        margin-bottom: 30px;
    }

    .btn-main {
        background: linear-gradient(90deg, #4b6cb7, #182848);
        color: #fff;
        text-decoration: none;
        padding: 12px 35px;
        border-radius: 30px;
        font-weight: 600;
        transition: 0.3s;
    }

        .btn-main:hover {
            background: linear-gradient(90deg, #182848, #4b6cb7);
            transform: scale(1.05);
        }

    .icons {
        font-size: 3rem;
        margin-bottom: 15px;
    }
</style>

<div class="home-container">
    <div class="icons">🚗</div>
    <h1>مرحباً بك في سريع منصة لإدارةالخطوط</h1>
    <p>
        يمكنك إدارة معلومات السواق والخطوط بكل سهولة واحترافية.
        اختر القسم الذي ترغب بالانتقال إليه أدناه 
    </p>

    <div>
        <a asp-controller="Drivers" asp-action="Index" class="btn-main mx-2">👥 قائمة السواق</a>
        <a asp-controller="Lines" asp-action="Index" class="btn-main mx-2">🚗 قائمة الخطوط</a>
    </div>
</div>